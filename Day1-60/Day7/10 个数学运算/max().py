# max(iterable,*[, key, default])

# *表示，后面的形参只能为关键字参数，不能为位置参数.default为返回
# 调用以下形式
# max(iterable)
# max(iterable,*, key)
max((),default =0) #0  # max(iterable,*,default)
# max(iterable,*, key, default)

