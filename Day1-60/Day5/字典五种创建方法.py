# 1.手动创建
empty = {}
dic = {'a':1,'b'=2,'c'=3}

# 2.使用dict()构造函数
dict(a=1,b=2,c=3) 
#{'a':1,'b'=2,'c'=3}

# 3.键值对(如'a':1) +关键字参数(如c=3)
dict({'a':1,'b':2},c=3,d=4)
#{'a':1,'b':2,'c'=3,'d':4}

# 4.可迭代对象
# 列表，元素又为一个元组，后面再加一系列关键字参数
dict([('a',1),('b',2)],c=3)
#{'a':1,'b'=2,'c'=3}

# 5.fromkeys()方法
# 已知键集合(keys),values 为初始值
{}.fromkeys(['k1','k2','k3'],[1,2,3])
#{'k1': [1, 2, 3], 'k2': [1, 2, 3], 'k3': [1, 2, 3]}

{'a':1,'b':2}.fromkeys(['c','d'],[1,2])
#{'c': [1, 2], 'd': [1, 2]}